default:
  supported-systems: [base16]
  filename: "./intermediate/{{ scheme-system }}-{{ scheme-slug }}.style.conf"

informations:
  supported-systems: [base16]
  filename: "src/themes/{{ scheme-system }}-{{ scheme-slug }}.json"

